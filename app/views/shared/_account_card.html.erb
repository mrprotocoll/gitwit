<div class="col-md-3">
    <div class="card">
        <div class="card-body d-flex align-items-center justify-content-between">
            <%= image_tag(account.image, class: "account-img rounded-circle", alt: account.name) %>
            <div>
                <h5 class="account-name"><%= account.name %></h5>
                <%= link_to "@#{account.username}", "https://twitter.com/#{account.username}" %>
            </div>
        </div>
        <%= button_to "Disconnect", account , method: :delete, class: "btn mt-0 m-3 btn-sm btn-danger btn-gradient" %>
    </div>
</div>